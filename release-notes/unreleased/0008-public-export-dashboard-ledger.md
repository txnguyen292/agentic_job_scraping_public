## Fixed

- Kept the internal ADK eval dashboard out of the public export, restored the Linear release ledger script required by exported tests, and made private context tests skip cleanly in public snapshots.
